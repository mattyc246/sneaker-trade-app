
	<%= form_for :trade, url: {action: "create"} do |f| %>
	<div class="form-group mt-4">
			<%= f.label 'Offer' %>
			<%= f.text_area :offer, class:"form-control"  %></br>
			<%= f.submit "Make Offer!", class:"btn btn-outline-success" %>
	</div>
	<% end %>